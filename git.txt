# 1. Check changed files
git status

# 2. Stage all modified files
git add .

# 3. Commit the changes
git commit -m "Fix job verification: add done_pending_verification to MySQL enum, enable admin verify/reject, and send verification/rejection emails"

# 4. Push to your main GitHub branch
git push origin main
